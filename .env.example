# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/live_website"

# AI Configuration
# Provider: google | openai
AI_PROVIDER=openai
# Model: gpt-4o-mini | gpt-4o | gemini-2.0-flash-exp
AI_MODEL=gpt-4o-mini
# API Key for chosen provider
# Google: https://aistudio.google.com/app/apikey
# OpenAI: https://platform.openai.com/api-keys
AI_API_KEY=your_api_key_here

# BetterAuth Configuration
# Base URL of your application (must include /api/auth)
BETTER_AUTH_URL="http://localhost:3000/api/auth"
# Secret key for auth (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET="your-secret-key-here-change-in-production"
# Relying Party ID for Passkeys (domain without protocol, e.g., "localhost" or "yourdomain.com")
BETTER_AUTH_RP_ID="localhost"
# Public URL for client-side usage
NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000/api/auth"

# Password Security
# Pepper key for password hashing (generate with: openssl rand -hex 32)
# CRITICAL: Keep this secret safe! If compromised, all passwords are at risk.
# NEVER commit this to version control or expose it publicly.
PASSWORD_PEPPER="your-password-pepper-here-change-in-production"
